---
phase: 07-secrets-gap-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shells/common/default.nix
  - Justfile
  - .github/workflows/nix-checks.yml
  - docs/secrets-workflow.md
autonomous: true
requirements:
  - SECR-01
  - SECR-02
gap_closure: true
must_haves:
  truths:
    - Repository has an explicit full-repo secret-scan mode in addition to changed-file scopes so legacy leaks are detectable before signoff.
    - Operator and CI command surfaces expose a stable full-scan signoff path without degrading fast feedback for PR diffs.
    - CI required signoff triggers are explicit: `push` to `main` and `workflow_dispatch` both invoke `SECRETS_SCAN_SCOPE=full`.
    - Full-scan guidance is documented as a required milestone/release closure step.
  artifacts:
    - path: src/shells/common/default.nix
      provides: secrets-scan supports `SECRETS_SCAN_SCOPE=full` and preserves staged/diff-base flows
      contains: SECRETS_SCAN_SCOPE
    - path: Justfile
      provides: canonical wrapper for full-repo secret scan
      contains: secrets-scan-full
    - path: .github/workflows/nix-checks.yml
      provides: CI path running full-repo scan for signoff surfaces
      contains: Secrets Scan
    - path: docs/secrets-workflow.md
      provides: operator signoff instructions for full-repo secret scan
      contains: secrets-scan-full
  key_links:
    - from: src/shells/common/default.nix
      to: Justfile
      via: shared scan mode is exposed through canonical wrapper commands
      pattern: secrets-scan
    - from: src/shells/common/default.nix
      to: .github/workflows/nix-checks.yml
      via: required `push` (`main`) and `workflow_dispatch` workflow paths invoke full-scope scan mode through existing scan command path
      pattern: SECRETS_SCAN_SCOPE: full
    - from: docs/secrets-workflow.md
      to: Justfile
      via: documentation maps signoff procedure to executable just command
      pattern: just secrets-scan-full
---

<objective>
Add and operationalize a full-repository secret-scan signoff path so milestone security guarantees are not limited to changed-file scope checks.

Purpose: Close the audit gap where diff-based guardrails could miss legacy plaintext leaks.
Output: Implemented `full` scan scope, wrapper command, CI signoff wiring, and operator runbook coverage.
</objective>

<execution_context>
@/Users/adampaterson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adampaterson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-secrets-gap-closure/07-RESEARCH.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@src/shells/common/default.nix
@Justfile
@.github/workflows/nix-checks.yml
@docs/secrets-workflow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement full-repository mode in shared secrets scan command</name>
  <files>src/shells/common/default.nix</files>
  <action>Extend scan scope handling to include `SECRETS_SCAN_SCOPE=full`, scanning tracked repository content from root while preserving existing `staged`, `diff-base`, and `working-tree` behavior for developer loops. Keep allowlist handling unchanged unless required for encrypted SOPS metadata placeholders.</action>
  <verify>Run `SECRETS_SCAN_SCOPE=full nix develop .#ci -c secrets-scan` and `SECRETS_SCAN_SCOPE=diff-base nix develop .#ci -c secrets-scan` to confirm both modes execute correctly.</verify>
  <done>`secrets-scan` accepts full scope and still supports existing diff-oriented modes without regression.</done>
</task>

<task type="auto">
  <name>Task 2: Add operator and CI signoff surfaces for full scan</name>
  <files>Justfile, .github/workflows/nix-checks.yml</files>
  <action>Add `just secrets-scan-full` as canonical local command and wire workflow conditions so full scan is required on exactly these signoff triggers: `push` to `main` and `workflow_dispatch`. Ensure both trigger paths set `SECRETS_SCAN_SCOPE=full`, while `pull_request` continues using diff-based scan defaults for fast feedback. Do not leave trigger behavior ambiguous.</action>
  <verify>Run `just --list | rg 'secrets-scan-full'` and verify `.github/workflows/nix-checks.yml` explicitly contains `on.push.branches: [main]` and `on.workflow_dispatch`, with secrets scan env configured so those triggers invoke `SECRETS_SCAN_SCOPE=full`.</verify>
  <done>Both local and CI paths include an explicit full-scan signoff route while preserving fast PR guardrails.</done>
</task>

<task type="auto">
  <name>Task 3: Document full-scan signoff policy for phase closure</name>
  <files>docs/secrets-workflow.md</files>
  <action>Update secret operations documentation to distinguish everyday diff scans from mandatory full-repo signoff scans. Include when to run each mode, expected failure handling, and required run timing before milestone closure.</action>
  <verify>Read `docs/secrets-workflow.md` and confirm it explicitly defines the signoff path using `just secrets-scan-full` plus escalation guidance on failures.</verify>
  <done>Documentation provides a clear and repeatable full-repo secret scan signoff policy tied to milestone/release closure.</done>
</task>

</tasks>

<verification>
Validate `full` and `diff-base` scan scopes in shell tooling, confirm `just secrets-scan-full` exists, and verify CI explicitly runs `SECRETS_SCAN_SCOPE=full` on `push` to `main` and `workflow_dispatch`.
</verification>

<success_criteria>
SECR-01 and SECR-02 closure work includes a mandatory full-repo secret-scan capability and operator-visible signoff contract that catches both new and legacy leaks.
</success_criteria>

<output>
After completion, create `.planning/phases/07-secrets-gap-closure/07-02-SUMMARY.md`
</output>
