---
phase: 07-secrets-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/home/security/secrets/default.nix
  - src/homes/x86_64-linux/adam@aurora/programs/openclaw/default.nix
  - secrets/hosts/aurora.yaml
  - .planning/phases/07-secrets-gap-closure/07-01-EVIDENCE.md
autonomous: true
requirements:
  - SECR-01
  - SECR-02
gap_closure: true
must_haves:
  truths:
    - Aurora OpenClaw auth remains runtime-secret sourced through a `tokenFile` path and has no plaintext token literal in tracked Nix config.
    - Required Home Manager secret declarations/assertions include the OpenClaw gateway auth key so apply/eval fails hard if it is missing.
    - Raw execution evidence captures commands proving runtime-only secret path handoff in evaluated `adam@aurora` home configuration.
  artifacts:
    - path: src/homes/x86_64-linux/adam@aurora/programs/openclaw/default.nix
      provides: OpenClaw gateway auth consumes runtime secret path reference without plaintext fallback
      contains: tokenFile
    - path: src/modules/home/security/secrets/default.nix
      provides: required secret declarations/assertions and session variable export for OpenClaw gateway auth token path
      contains: hosts/aurora/openclaw/gateway_auth_token
    - path: secrets/hosts/aurora.yaml
      provides: encrypted host-scoped OpenClaw gateway auth token source
      contains: hosts.aurora.openclaw.gateway_auth_token
    - path: .planning/phases/07-secrets-gap-closure/07-01-EVIDENCE.md
      provides: raw command output and grep evidence from implementation execution without authoritative requirement-status updates
      contains: runtime-only OpenClaw secret evidence
  key_links:
    - from: src/modules/home/security/secrets/default.nix
      to: src/homes/x86_64-linux/adam@aurora/programs/openclaw/default.nix
      via: exported runtime secret path/env handoff consumed by OpenClaw gateway auth
      pattern: OPENCLAW_GATEWAY_AUTH_TOKEN_FILE
    - from: secrets/hosts/aurora.yaml
      to: src/modules/home/security/secrets/default.nix
      via: encrypted key is declared and required in Home Manager secrets profile
      pattern: hosts/aurora/openclaw/gateway_auth_token
---

<objective>
Lock and prove runtime-only OpenClaw secret consumption so audit blockers around plaintext token usage cannot regress.

Purpose: Close the SECR requirement gap tied to OpenClaw auth wiring and align raw implementation evidence capture with current secure implementation.
Output: Hardened runtime-secret-only OpenClaw path with raw implementation evidence capture for downstream authoritative verification.
</objective>

<execution_context>
@/Users/adampaterson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adampaterson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-secrets-gap-closure/07-RESEARCH.md
@.planning/v1.0-v1.0-MILESTONE-AUDIT.md
@src/modules/home/security/secrets/default.nix
@src/homes/x86_64-linux/adam@aurora/programs/openclaw/default.nix
@secrets/hosts/aurora.yaml
@.planning/phases/07-secrets-gap-closure/07-01-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce runtime-only OpenClaw token wiring and remove literal fallback paths</name>
  <files>src/modules/home/security/secrets/default.nix, src/homes/x86_64-linux/adam@aurora/programs/openclaw/default.nix, secrets/hosts/aurora.yaml</files>
  <action>Ensure the OpenClaw gateway token is defined only as encrypted SOPS data, declared in Home Manager secrets with required-key assertions, and consumed via runtime `tokenFile` path handoff. Remove any inline token literal or plaintext fallback in OpenClaw config so all credential materialization happens at activation/deploy time.</action>
  <verify>Run `nix eval .#homeConfigurations."adam@aurora".config.sops.secrets."hosts/aurora/openclaw/gateway_auth_token".path` and `nix build --dry-run .#homeConfigurations."adam@aurora".activationPackage`.</verify>
  <done>OpenClaw auth has no plaintext credential literal in tracked config and runtime secret references evaluate/build correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add anti-regression checks for plaintext token reintroduction</name>
  <files>src/homes/x86_64-linux/adam@aurora/programs/openclaw/default.nix, .planning/phases/07-secrets-gap-closure/07-01-EVIDENCE.md</files>
  <action>Add explicit anti-regression checks that grep for token-like literals in OpenClaw configuration path and confirm token consumption references runtime secret files only. Record raw command outputs in `07-01-EVIDENCE.md` without updating authoritative verification status documents; authoritative requirement closure remains reserved for Plan 07-03.</action>
  <verify>Run `rg -n --hidden --glob '!*.age' --glob '!*.yaml' 'gateway_auth_token|OPENCLAW|token\\s*=\\s*\"' src/homes/x86_64-linux/adam@aurora/programs/openclaw/default.nix src/modules/home/security/secrets/default.nix` and append expected-safe output to `.planning/phases/07-secrets-gap-closure/07-01-EVIDENCE.md`.</verify>
  <done>Raw evidence file includes reproducible anti-regression output proving OpenClaw token literals are absent in tracked Nix config, with no authoritative verification document edits in this plan.</done>
</task>

</tasks>

<verification>
Prove OpenClaw secret handling is runtime-only by evaluating the SOPS secret path, dry-building aurora home activation, and running anti-regression literal detection checks.
</verification>

<success_criteria>
SECR-01 and SECR-02 evidence for the OpenClaw path is restored: encrypted source in git, runtime-only decryption handoff, and no plaintext credential literals in evaluated Nix expressions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-secrets-gap-closure/07-01-SUMMARY.md`
</output>
