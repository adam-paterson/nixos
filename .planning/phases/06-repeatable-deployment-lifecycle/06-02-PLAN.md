---
phase: 06-repeatable-deployment-lifecycle
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .github/workflows/deploy-aurora.yml
  - docs/aurora-deploy-rollback.md
autonomous: true
requirements:
  - DEPL-01
must_haves:
  truths:
    - User can deploy aurora updates through one canonical workflow with explicit target ref selection.
    - User can execute rollback using repository-defined commands for both immediate generation rollback and prior-ref redeploy.
    - User can capture deterministic deploy/rollback verification evidence after each lifecycle action.
  artifacts:
    - path: .github/workflows/deploy-aurora.yml
      provides: deterministic aurora deploy workflow with explicit ref-driven activation behavior
      contains: workflow_dispatch.inputs.ref
    - path: docs/aurora-deploy-rollback.md
      provides: operator runbook for forward deploy and rollback procedures with verification checklist
      contains: Rollback Procedure
  key_links:
    - from: docs/aurora-deploy-rollback.md
      to: .github/workflows/deploy-aurora.yml
      via: runbook uses workflow dispatch ref input as canonical remote deploy/rollback selector
      pattern: workflow_dispatch
    - from: docs/aurora-deploy-rollback.md
      to: Justfile
      via: runbook gates deploy/rollback actions through `just pre-apply-check`
      pattern: pre-apply-check
---

<objective>
Harden the aurora deployment lane into a rollback-capable operational contract with explicit forward-deploy and rollback procedures tied to deterministic refs and generation evidence.

Purpose: Fulfill DEPL-01 by ensuring VPS updates and recovery actions are both repository-defined and verifiable.
Output: Deploy workflow contract hardening plus a dedicated aurora deploy/rollback runbook.
</objective>

<execution_context>
@/Users/adampaterson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adampaterson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repeatable-deployment-lifecycle/06-RESEARCH.md
@.planning/phases/06-repeatable-deployment-lifecycle/06-01-PLAN.md
@.github/workflows/deploy-aurora.yml
@Justfile
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden ref-pinned aurora deploy workflow metadata and guardrails</name>
  <files>.github/workflows/deploy-aurora.yml</files>
  <action>Refine the workflow so manual dispatch and workflow-run deploys produce explicit deploy metadata (selected ref, resolved store path, and agent target) that can be audited and reused for rollback decisions. Preserve the existing Cachix activation lane; do not introduce alternative deployment systems or direct SSH orchestration.</action>
  <verify>
    <automated>actionlint .github/workflows/deploy-aurora.yml || yamllint .github/workflows/deploy-aurora.yml</automated>
  </verify>
  <done>Deploy workflow preserves one canonical aurora lane and exposes enough metadata to support deterministic rollback operations.</done>
</task>

<task type="auto">
  <name>Task 2: Codify aurora forward deploy and rollback procedures</name>
  <files>docs/aurora-deploy-rollback.md</files>
  <action>Create an operator-focused runbook that defines: required preflight gate (`just pre-apply-check`), forward deploy options (CI dispatch by ref and local host switch path), rollback paths (`sudo nixos-rebuild switch --rollback` and prior-known-good ref redeploy), and post-action verification commands (`nixos-rebuild list-generations`, service status, network reachability).</action>
  <verify>
    <automated>rg -n "pre-apply-check|switch --rollback|workflow_dispatch|list-generations" docs/aurora-deploy-rollback.md</automated>
  </verify>
  <done>Aurora deploy and rollback are documented as executable, deterministic procedures with explicit verification steps.</done>
</task>

</tasks>

<verification>
Validate the deploy workflow contract and confirm the runbook provides command-complete forward and rollback procedures with auditable evidence requirements.
</verification>

<success_criteria>
DEPL-01 is satisfied when aurora updates and rollback actions follow one repository-defined workflow with explicit, repeatable verification evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repeatable-deployment-lifecycle/06-02-SUMMARY.md`
</output>
