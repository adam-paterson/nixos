---
phase: 06-repeatable-deployment-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/systems/x86_64-linux/aurora/hardware/default.nix
  - Justfile
  - docs/aurora-bootstrap.md
  - .planning/phases/06-repeatable-deployment-lifecycle/06-01-EVIDENCE.md
autonomous: true
requirements:
  - DEPL-02
must_haves:
  truths:
    - User can bootstrap or reprovision `aurora` from repository-defined commands without ad hoc host-side edits.
    - User can verify post-bootstrap host readiness before any steady-state deployment step.
    - User can keep Hetzner disk and boot assumptions explicit in repository-managed host facts.
  artifacts:
    - path: src/systems/x86_64-linux/aurora/hardware/default.nix
      provides: explicit aurora hardware and filesystem contract for deterministic reprovisioning
      contains: fileSystems."/".device
    - path: Justfile
      provides: canonical bootstrap and bootstrap-verification command wrappers for aurora
      contains: aurora-bootstrap
    - path: docs/aurora-bootstrap.md
      provides: end-to-end Hetzner reprovision runbook mapped to repository commands and verification evidence
      contains: Post-bootstrap verification
    - path: .planning/phases/06-repeatable-deployment-lifecycle/06-01-EVIDENCE.md
      provides: reproducible bootstrap rehearsal evidence (command transcript and expected outputs)
      contains: Bootstrap rehearsal transcript
  key_links:
    - from: Justfile
      to: docs/aurora-bootstrap.md
      via: wrapper recipes execute documented bootstrap and verification sequence
      pattern: aurora-bootstrap
    - from: docs/aurora-bootstrap.md
      to: src/systems/x86_64-linux/aurora/hardware/default.nix
      via: runbook references the host hardware contract that provisioning must satisfy
      pattern: hardware/default.nix
---

<objective>
Establish a deterministic, repository-defined bootstrap/reprovision contract for the Hetzner VPS (`aurora`) so a fresh host can be recreated and validated without undocumented manual drift.

Purpose: Fulfill DEPL-02 by turning aurora provisioning into one canonical command path plus explicit post-bootstrap verification evidence.
Output: Finalized host hardware contract, bootstrap command wrappers, and an authoritative aurora bootstrap runbook.
</objective>

<execution_context>
@/Users/adampaterson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adampaterson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-repeatable-deployment-lifecycle/06-RESEARCH.md
@Justfile
@src/systems/x86_64-linux/aurora/hardware/default.nix
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize aurora hardware contract for reproducible provisioning</name>
  <files>src/systems/x86_64-linux/aurora/hardware/default.nix</files>
  <action>Replace placeholder comments and implicit assumptions with explicit Hetzner-targeted boot/filesystem guidance in the module contract (disk identifiers, filesystem expectations, and required update points during initial host discovery). Keep the module deterministic and avoid introducing host-imperative one-off values that cannot be regenerated.</action>
  <verify>
    <automated>nix eval .#nixosConfigurations.aurora.config.fileSystems."/".device --raw</automated>
  </verify>
  <done>Aurora hardware module documents and encodes a concrete, reproducible boot/filesystem contract suitable for reprovisioning.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical aurora bootstrap wrappers in Justfile</name>
  <files>Justfile</files>
  <action>Add explicit recipes for aurora bootstrap/reprovision and post-bootstrap verification (for example, invoking `nixos-anywhere` with repo flake target and a follow-up verification recipe). Keep existing apply/deploy recipes intact; this task adds a dedicated bootstrap lane rather than replacing steady-state deployment commands.</action>
  <verify>
    <automated>just --list | rg 'aurora-bootstrap|aurora-bootstrap-verify'</automated>
    <automated>just --dry-run aurora-bootstrap | rg 'nixos-anywhere|nixos-rebuild|#aurora'</automated>
  </verify>
  <done>Operators have one discoverable, repository-defined command surface to bootstrap and verify aurora provisioning.</done>
</task>

<task type="auto">
  <name>Task 3: Write an authoritative aurora reprovision runbook</name>
  <files>docs/aurora-bootstrap.md, .planning/phases/06-repeatable-deployment-lifecycle/06-01-EVIDENCE.md</files>
  <action>Create a focused runbook covering prerequisites, bootstrap command usage, required secrets/auth preflight, and post-bootstrap checks (generation list, service status, and connectivity smoke checks). Include exact command examples aligned with new Justfile wrappers and call out non-goals (no parallel deploy systems). Capture a bootstrap rehearsal transcript in `06-01-EVIDENCE.md` that records dry-run output and post-bootstrap verification commands/expected outputs.</action>
  <verify>
    <automated>rg -n "aurora-bootstrap|Post-bootstrap verification|nixos-anywhere" docs/aurora-bootstrap.md</automated>
    <automated>rg -n "Bootstrap rehearsal transcript|just --dry-run aurora-bootstrap|Expected output" .planning/phases/06-repeatable-deployment-lifecycle/06-01-EVIDENCE.md</automated>
  </verify>
  <done>The repository contains a complete, executable aurora reprovision procedure with explicit verification evidence requirements.</done>
</task>

</tasks>

<verification>
Pass criteria:
- Bootstrap wrappers are discoverable and dry-run rehearsal confirms expected command path.
- Aurora host eval/hardware contract resolves without missing root filesystem contract.
- Runbook provides a complete from-scratch reprovision path with post-bootstrap checks.
- `06-01-EVIDENCE.md` captures rehearsal transcript and expected verification outputs.
</verification>

<success_criteria>
DEPL-02 is satisfied when aurora can be bootstrapped/reprovisioned from repository-defined commands and validated through documented post-bootstrap evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/06-repeatable-deployment-lifecycle/06-01-SUMMARY.md`
</output>
