---
phase: 05-validation-and-change-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .github/workflows/nix-checks.yml
  - docs/validation-gates.md
  - README.md
autonomous: true
requirements:
  - VALD-02
must_haves:
  truths:
    - User can rely on a single required CI workflow that blocks unsafe merges when checks fail.
    - User can see explicit CI coverage for format, lint, flake check, host eval, host dry-build, and secrets scan.
    - User can verify merge-gate policy state with a reproducible GitHub CLI command.
  artifacts:
    - path: .github/workflows/nix-checks.yml
      provides: required merge-gate workflow with explicit build-depth checks
      contains: Host Contract Dry Build
    - path: docs/validation-gates.md
      provides: operator policy for required status checks and enforcement verification
      contains: gh api repos
    - path: README.md
      provides: top-level references to CI gate policy and required workflow semantics
      contains: validation-gates.md
  key_links:
    - from: .github/workflows/nix-checks.yml
      to: src/shells/common/default.nix
      via: CI invokes shared scripts using `nix develop .#ci -c ...`
      pattern: nix develop .#ci -c
    - from: docs/validation-gates.md
      to: .github/workflows/nix-checks.yml
      via: policy names exact workflow/job status expected as required
      pattern: Nix Checks
---

<objective>
Convert CI checks from advisory signals into an explicit merge-blocking quality gate with clear build-depth expectations and auditable enforcement checks.

Purpose: Fulfill VALD-02 by ensuring unsafe changes cannot merge when required quality checks fail.
Output: Hardened `Nix Checks` workflow contract, enforcement runbook, and top-level documentation linkage.
</objective>

<execution_context>
@/Users/adampaterson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adampaterson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-validation-and-change-gates/05-RESEARCH.md
@.planning/phases/05-validation-and-change-gates/05-01-PLAN.md
@.github/workflows/nix-checks.yml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Nix Checks workflow as the required merge gate</name>
  <files>.github/workflows/nix-checks.yml</files>
  <action>Keep `Nix Checks` as the single merge-gate workflow and ensure it executes explicit quality and build-depth stages: format, lint, flake checks, host eval, host dry-build contract, and secrets scan. Preserve trigger policy (`pull_request`, `push` to `main`, `workflow_dispatch`) while keeping event-specific secrets scan scope behavior deterministic.</action>
  <verify>Run `actionlint .github/workflows/nix-checks.yml` if available (or `nix develop .#ci -c check` plus YAML lint command in repo tooling), then validate workflow file includes a host dry-build step (for example `nix develop .#ci -c flake-contract`).</verify>
  <done>`Nix Checks` expresses full required CI gate coverage, including host dry-build depth and existing integrity/security checks.</done>
</task>

<task type="auto">
  <name>Task 2: Add CI gate enforcement runbook with GitHub protection verification</name>
  <files>docs/validation-gates.md</files>
  <action>Create a concise runbook defining required-check policy for `main` and include reproducible enforcement verification commands for both branch protection and/or rulesets using GitHub CLI/API. Include expected pass/fail behavior and how to detect misconfiguration that would allow merges on failed checks.</action>
  <verify>Read `docs/validation-gates.md` and confirm it includes at least one concrete command for enforcement verification (for example `gh api repos/<owner>/<repo>/branches/main/protection/required_status_checks`).</verify>
  <done>Repository contains an auditable policy document that turns CI checks into enforceable merge blockers.</done>
</task>

<task type="auto">
  <name>Task 3: Link CI gate policy from top-level documentation</name>
  <files>README.md</files>
  <action>Update README automation/validation sections to reference `docs/validation-gates.md` as the authoritative merge-gate policy and to state that `Nix Checks` must be configured as required status for protected merges.</action>
  <verify>Read `README.md` and confirm it links `docs/validation-gates.md` and states merge-blocking intent for `Nix Checks`.</verify>
  <done>Top-level docs route operators/reviewers to one authoritative CI gate policy source.</done>
</task>

</tasks>

<verification>
Confirm the workflow contains explicit build-depth coverage and that documentation provides a reproducible method to verify required-status enforcement on `main`.
</verification>

<success_criteria>
VALD-02 is satisfied when failed CI gate runs block merge eligibility and the required-check policy is transparent and auditable.
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-and-change-gates/05-02-SUMMARY.md`
</output>
