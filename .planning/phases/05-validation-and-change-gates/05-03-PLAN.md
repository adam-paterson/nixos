---
phase: 05-validation-and-change-gates
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - docs/flake-lock-workflow.md
  - .github/pull_request_template.md
  - README.md
autonomous: true
requirements:
  - VALD-03
must_haves:
  truths:
    - User can update a single flake input intentionally with reviewable `flake.lock` diffs.
    - User can distinguish lock verify path from lock update path during normal feature work.
    - Reviewers can enforce lockfile intent and post-update validation through a repeatable PR checklist.
  artifacts:
    - path: docs/flake-lock-workflow.md
      provides: authoritative lock verify/update policy with scoped-update and validation checklist
      contains: Lock Diff Review Checklist
    - path: .github/pull_request_template.md
      provides: reviewer-visible lockfile checklist enforcing scoped intent and validation evidence
      contains: flake.lock
    - path: README.md
      provides: top-level route to lock governance policy and canonical commands
      contains: lock-verify
  key_links:
    - from: README.md
      to: docs/flake-lock-workflow.md
      via: canonical command docs defer to full lock governance policy
      pattern: docs/flake-lock-workflow.md
    - from: .github/pull_request_template.md
      to: docs/flake-lock-workflow.md
      via: PR checklist references same policy constraints for review
      pattern: lock
---

<objective>
Formalize lockfile maintenance governance so input updates are explicit, scoped, and reviewable instead of incidental side effects.

Purpose: Fulfill VALD-03 by turning existing lock commands into a strict workflow contract with consistent reviewer enforcement.
Output: Hardened lock workflow policy, PR-level review checklist, and aligned top-level command guidance.
</objective>

<execution_context>
@/Users/adampaterson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adampaterson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-validation-and-change-gates/05-RESEARCH.md
@.planning/phases/05-validation-and-change-gates/05-01-PLAN.md
@docs/flake-lock-workflow.md
@README.md
@Justfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden lock workflow policy with explicit scoped-update governance</name>
  <files>docs/flake-lock-workflow.md</files>
  <action>Refine the lock workflow doc so verify (`lock-verify`) and update (`lock-update <input>`) paths are explicitly separated with required intent statements, scoped update expectations, and post-update validation commands (`just ci`, `just lock-verify`). Keep policy focused on reviewable diffs and avoid introducing broad automated update churn in Phase 5.</action>
  <verify>Read `docs/flake-lock-workflow.md` and confirm it includes: scoped input update command, lock diff review requirements, and post-update validation steps.</verify>
  <done>Lock workflow policy is explicit enough that feature PRs cannot justify incidental lock churn.</done>
</task>

<task type="auto">
  <name>Task 2: Add PR checklist enforcing lockfile review requirements</name>
  <files>.github/pull_request_template.md</files>
  <action>Create a pull request template section that requires authors to declare whether `flake.lock` changed intentionally, identify targeted inputs, summarize diff scope, and confirm validation command evidence. Keep checklist concise so it is likely to be used consistently.</action>
  <verify>Open `.github/pull_request_template.md` and confirm lock-specific checklist items exist for intent, scope, diff review, and validation evidence.</verify>
  <done>PR reviews have a built-in lock governance checklist that reinforces controlled update behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Align top-level docs with controlled lock update contract</name>
  <files>README.md</files>
  <action>Update README lockfile section to call out `just lock-verify` as the normal path and `just lock-update <input>` as explicit maintenance. Link to the lock workflow policy and note that lock diffs must be reviewed and validated before merge.</action>
  <verify>Read `README.md` and confirm lock section distinguishes verify vs update and links to `docs/flake-lock-workflow.md` policy requirements.</verify>
  <done>Top-level documentation consistently reflects controlled lock update governance and reviewable diff expectations.</done>
</task>

</tasks>

<verification>
Validate that lock governance is documented in one authoritative policy and reinforced through PR checklist and top-level docs.
</verification>

<success_criteria>
VALD-03 is satisfied when lock updates are intentional, scoped, evidence-backed changes with reviewable `flake.lock` diffs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-and-change-gates/05-03-SUMMARY.md`
</output>
