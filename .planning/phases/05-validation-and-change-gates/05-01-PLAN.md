---
phase: 05-validation-and-change-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shells/common/default.nix
  - Justfile
  - README.md
autonomous: true
requirements:
  - VALD-01
must_haves:
  truths:
    - User can run one canonical local gate command before apply/deploy that includes `nix flake check`.
    - User can verify host-level eval plus dry-build coverage from the same local validation contract.
    - User can follow a documented pre-apply sequence without guessing command order.
  artifacts:
    - path: src/shells/common/default.nix
      provides: deterministic command contract for `check`, `eval`, and host dry-build validation (`flake-contract`)
      contains: flake-contract.exec
    - path: Justfile
      provides: canonical local validation entrypoint wrapping CI-shell commands
      contains: pre-apply-check
    - path: README.md
      provides: operator-facing pre-apply gate instructions aligned to executable commands
      contains: pre-apply-check
  key_links:
    - from: Justfile
      to: src/shells/common/default.nix
      via: wrapper calls into CI-shell scripts
      pattern: just _run
    - from: Justfile
      to: README.md
      via: documented command maps directly to executable recipe
      pattern: pre-apply-check
---

<objective>
Harden the local pre-apply validation contract so every operator run includes `nix flake check` plus explicit host eval/dry-build coverage before activation or deployment.

Purpose: Fulfill VALD-01 with a deterministic, low-ambiguity command surface that is consistent with repository CI-shell behavior.
Output: Canonical local validation recipe, script-level gate ordering, and README guidance mapped to executable commands.
</objective>

<execution_context>
@/Users/adampaterson/.codex/get-shit-done/workflows/execute-plan.md
@/Users/adampaterson/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-validation-and-change-gates/05-RESEARCH.md
@src/shells/common/default.nix
@Justfile
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define a strict pre-apply gate script contract in shared shell tooling</name>
  <files>src/shells/common/default.nix</files>
  <action>Keep `check.exec` (`nix flake check "path:$PWD" --show-trace`) as the canonical integrity gate, then harden `flake-contract.exec` usage notes and ordering so host eval + dry-build checks are an explicit required layer for pre-apply confidence. Do not remove or bypass existing `fmt-check`, `lint`, or `eval` scripts.</action>
  <verify>Run `nix develop .#ci -c check`, `nix develop .#ci -c eval`, and `nix develop .#ci -c flake-contract`.</verify>
  <done>Shared shell scripts provide an unambiguous validation sequence with `nix flake check` and host dry-build assurance available as first-class commands.</done>
</task>

<task type="auto">
  <name>Task 2: Add one local pre-apply entrypoint in Justfile</name>
  <files>Justfile</files>
  <action>Add a `pre-apply-check` recipe that runs `fmt-check`, `lint`, `check`, `eval`, `flake-contract`, and `secrets-scan` via the existing `_run`/CI-shell pattern. Keep existing recipes intact for selective use; this task introduces a canonical full-gate entrypoint instead of replacing granular commands.</action>
  <verify>Run `just --list | rg 'pre-apply-check'` and `just pre-apply-check`.</verify>
  <done>Operators can execute one deterministic pre-apply validation command that includes `nix flake check` and host dry-build checks.</done>
</task>

<task type="auto">
  <name>Task 3: Document canonical pre-apply gate usage for operators</name>
  <files>README.md</files>
  <action>Update command documentation to declare `just pre-apply-check` as the required pre-apply/deploy gate. Explicitly state that `nix flake check` is mandatory and that host dry-build validation runs through `flake-contract` in the same command path.</action>
  <verify>Read `README.md` and confirm the pre-apply section names `just pre-apply-check` and references `nix flake check` plus host dry-build coverage.</verify>
  <done>Repository docs define one canonical local gate contract aligned with executable command behavior.</done>
</task>

</tasks>

<verification>
Run the canonical local gate command and verify it executes `check` (`nix flake check`), host evaluation, and host dry-build validation in a deterministic sequence.
</verification>

<success_criteria>
VALD-01 is satisfied with a clearly documented and executable local validation gate that operators run before any apply/deploy action.
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation-and-change-gates/05-01-SUMMARY.md`
</output>
